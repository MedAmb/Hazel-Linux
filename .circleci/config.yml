version: 2.1
jobs:
  BUILD:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Build"
          command: "make all D=0"

workflows:
  say-hello-workflow:
    jobs:
      - BUILD
